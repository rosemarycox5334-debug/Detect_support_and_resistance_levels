# 关键位"有效概率"显示方案（含实测数据，供选择）

> 全部数字来自 `out/final` 的走查回测：130 只训练股票 / 130 只**互斥的**测试股票，
> 2012-2026，support 测试集 20,064 次已判定触及、resistance 24,554 次。
> 复现：`python scripts/prob_model.py out/final`

---

## 0. 先看清楚：三种"概率"的可预测性差别巨大

我原来只做了"守住概率"，而且故意不用距离和带宽（因为要检验算法定位能力）。
把口径换成"给用户预测"之后，重新测了三个维度：

| 要预测的东西 | AUC | 预测值范围 | P10~P90 跨度 | 校准误差 | 判断 |
|---|---|---|---|---|---|
| **① 触及概率**（未来40根内价格会不会走到这个位） | **0.77 / 0.73** | 6.5% ~ 95.3% | **61.1pp / 52.1pp** | — | ✅ 强，非常可用 |
| **② 守住概率**（触及后会不会反弹） | 0.59 / 0.61 | 53.8% ~ 85.3% | 15.2pp / 17.9pp | **< 2pp** | ⚠️ 弱但校准很好 |
| ③ 我原来的排序分 | 0.54 / 0.56 | 70.3% ~ 76.2% | 5.9pp / 8.4pp | — | ❌ 跨度太小，你说的"都差不多"就是这个 |

（斜杠前是 support，后是 resistance）

**两个关键结论：**

1. **触及概率很好预测**（AUC 0.77）。这很直觉：距离越近越容易走到。
   而且跨度 61pp —— 用户能清楚看到 15% 和 90% 的差别。
2. **守住概率确实弱**（AUC 0.59），但**校准非常好**。实测校准表：

   | 模型说 | 实际是 | 样本数 |
   |---|---|---|
   | 58.4% | 60.5% | 516 |
   | 65.9% | 64.4% | 4,067 |
   | 75.1% | 75.4% | 13,141 |
   | 81.3% | 82.3% | 2,340 |

   误差都在 2pp 以内。也就是说这个数字**不会骗人**，只是区分度有限。
   加上距离和带宽后跨度从 5.9pp 提到 15.2pp，比原来好 2.6 倍。

---

## 方案 A：单一"守住概率"

界面显示：

```
支撑带 9.96    守住概率 74%
```

- **实现**：逻辑回归（距离、距离²、带宽、测试次数、陈旧度、成交量剖面、ATR/价格），
  train 拟合 → 落盘系数 → 线上直接算
- **数据支撑**：AUC 0.587/0.612，校准误差 <2pp，范围 54%~85%
- **优点**：最直接回答你的问题，一个数字，用户零学习成本
- **缺点**：大部分位会落在 70%~80%，你之前说的"看着都差不多"仍然存在（虽然比原来好 2.6 倍）
- **工作量**：小（1 个新文件 + 界面加一列）

---

## 方案 B：双概率（触及 + 守住）✅ 我推荐这个

界面显示：

```
支撑带 9.96    触及概率 82%  ·  守住概率 74%
```

- **为什么**：这两件事用户都需要知道，而且**一个强一个弱正好互补**。
  一个"守住概率 85%"但"触及概率 8%"的位，实际上用不上（价格根本走不到）。
  实测触及概率范围 6.5%~95.3%，这个差异是真实且巨大的。
- **数据支撑**：触及 AUC 0.770/0.732；守住 AUC 0.587/0.612 且校准 <2pp
- **优点**：区分度最强的信息（触及概率）也给了用户；两个数字含义清晰不重叠
- **缺点**：两个数字，用户要理解一下
- **工作量**：小（两个逻辑回归，同一套特征）

---

## 方案 C：合成一个"有效概率" = 触及 × 守住

界面显示：

```
支撑带 9.96    有效概率 61%
```

- **含义**：未来 40 根内，价格走到这个位**并且**守住的联合概率
- **数据支撑**：由 B 的两个模型相乘。因为触及概率跨度 61pp，
  合成后的跨度远大于单看守住概率（估算 5%~80%）
- **优点**：一个数字，且区分度大，用户一眼能排序
- **缺点**：把两件事揉在一起了。"有效概率 20%"可能是"走不到"也可能是"会跌穿"，
  用户不知道是哪种，决策含义不同
- **工作量**：小（在 B 基础上乘一下）

---

## 方案 D：概率 + 置信区间 + 样本数

界面显示：

```
支撑带 9.96    守住概率 74%  (±1.5pp, n=13141)
```

- **优点**：最诚实。用户能看出"这个数字有多可信"
- **缺点**：信息密度高，普通用户可能反而更困惑
- **工作量**：小（Wilson 区间已实现）

---

## 方案 E：强弱标签（隐藏数字）

界面显示：

```
支撑带 9.96    强度 ★★★☆☆      （鼠标悬浮显示"守住概率 74%，历史 13141 次验证"）
```

- **映射**：按预测概率分 5 级（<62% / 62-70% / 70-76% / 76-82% / >82%），
  边界取自 train 集分位数
- **优点**：避免用户过度相信一个精确到百分位的数字；视觉上排序最快
- **缺点**：损失信息；本质还是我之前被你否掉的分档，只是换了皮
- **工作量**：小

---

## 方案 F：期望收益（不用概率）

界面显示：

```
支撑带 9.96    触及后10日期望 +0.96%
```

- **数据支撑**（support，test 集，实测分层）：

  | 距现价 | 测试 0-3次 | 4-8次 | 9-15次 | 16+次 |
  |---|---|---|---|---|
  | 0.5-1.5 ATR | +0.03% | +0.37% | +0.43% | +0.47% |
  | 1.5-2.5 ATR | +0.68% | +0.48% | +0.65% | +0.47% |
  | 2.5-3.5 ATR | +0.49% | +0.37% | +1.14% | +0.94% |
  | 3.5-5.0 ATR | **+1.66%** | +1.14% | +0.96% | +0.96% |

  梯度清楚：越远的支撑位，触及后反弹幅度越大（+0.03% → +1.66%）

- **优点**：直接是钱，最可操作，不需要用户理解概率
- **缺点**：期望收益的方差很大（单次可能 -10% 也可能 +15%），
  只显示均值容易让人以为稳赚。压力位那边全是负数（A股 长期上行漂移），
  意味着"做空压力位"不赚钱，显示出来可能反而困惑
- **工作量**：小

---

## 方案 G：组合面板（信息最全）

界面显示：

```
支撑带 9.96 ~ 10.00   (带宽 0.30 ATR，距现价 -4.89% / -2.02 ATR)
├ 触及概率   82%   价格未来40天走到这里的可能性
├ 守住概率   74%   走到后反弹的可能性（历史校准，±1.5pp）
├ 有效概率   61%   两者兼具
├ 期望收益  +0.96% 触及后持有10天的历史均值
└ 历史验证    4 次  这个位被测试过的次数
```

- **优点**：一次给全，用户按自己需要看
- **缺点**：占地方；和你"看着太乱"的诉求相反
- **工作量**：中（可以做成默认收起、点击展开）

---

## 我的建议

**选 B（双概率）**，理由：

1. 触及概率是三个维度里唯一 AUC 上 0.7 的，跨度 61pp，用户能真正看出差别 ——
   这是目前数据里最有信息量的东西，不用它太浪费
2. 守住概率虽弱（AUC 0.59），但校准误差 <2pp，**这个数字不会骗人**，
   而且加了距离/带宽后跨度从 5.9pp 提到 15.2pp，已经比之前可用得多
3. 两个数字含义不重叠，不像方案 C 那样把"走不到"和"会跌穿"混在一起

如果你觉得两个数字还是多，就选 **C**（合成一个），但我会在悬浮提示里给出分解。

如果你想要最简单，选 **A**，代价是区分度只有 15pp。

---

## 必须诚实说明的两点

无论选哪个方案，这两条都成立：

1. **守住概率的排序能力弱**（AUC 0.59）。它的绝对值可信（校准好），
   但用它来比较两个相邻的位（比如 74% vs 77%）意义不大，噪声就有这么大。
2. **概率是"同类历史统计"，不是对这一次的预测**。
   模型说 75% 的那批位，历史上确实 75.4% 守住了 —— 但具体到你眼前这一次，
   它要么守住要么破，没有中间态。
