# 支撑/阻力位识别 —— 精度优化方案（含实测验证）

> 全部数字来自本仓库代码在 **真实 A股 日线** 上的走查回测：
> 数据 `D:\A股_K线数据\parquet\stocks`，200 只股票（100 训练 / 100 测试，按代码 md5 分桶互斥），
> 2012-01-01 ~ 2026-07-07，每 20 根 K 线一个决策点，共 **15,280 个决策点**，
> 测试集 **363,041 条关键位观测**。
> 复现命令见文末第 7 节。

---

## 1. 一句话结论

现有算法报出的"历史守住率 87~92%"**几乎全部是区间画得太宽造成的统计假象**；
在控制了区间宽度和距现价距离之后，改进后的算法（V2/V3）
**无法把真实关键位与"把同一个位随机平移 1~2 个 ATR"的对照组区分开**
（CMH 分层检验 p = 0.38 ~ 0.58）。
唯一的例外是 V1 那些罕见的窄区间（support +5.33pp，p=0.0053），但它只覆盖约 4% 的决策点。

但有三件事是**真实且可利用**的：

| 能力 | 实测结论 | 建议 |
|---|---|---|
| **排序** 哪个位更值得看 | 两因子评分把守住率从 71.7% 拉到 78.4%，收益率从 −0.36% 拉到 +0.12%，训练/测试集一致 | ✅ 已上线 |
| **定位** 价格会在哪停 | 未来摆动极值的中位误差从 1.77 ATR 降到 1.09 ATR（−38%），且优于自身对照组的 1.37 ATR | ✅ 已上线，按 ATR 报误差 |
| **概率** 这个位会不会守住 | Brier 0.1828 vs 常数预测 0.1834，**零区分度** | ❌ 已下线"守住率 XX%"，改为可追溯的分位档 |

产品含义：**支撑/阻力不是一根线，而是一个约 1.5~2 ATR 宽的区域。**
应该卖"哪几个区域重要 + 区域有多宽"，不应该卖"精确到 0.01 元的价格 + 胜率百分比"。

---

## 2. 现状审计：6 个已实测确认的缺陷

以下每条都用 000001 平安银行日线（8,403 根）实测确认，不是读代码猜的。

### 2.1 成交量分布不守恒 —— 密度剖面被放大 15.8 倍

`density_analyzer.compute_density_profile` 第 98 行：

```python
density[lo_idx:hi_idx + 1] += w      # w = 当根K线的全额成交量
```

一根 K 线的成交量被**完整**计入它覆盖的每一个价格箱。40 日窗口实测：

- 每根 K 线覆盖 6 ~ 27 个箱（中位 14）
- 密度剖面隐含的总成交量 = 真实总量 × **15.8**
- 振幅大的 K 线权重被额外放大：2026-06-12 振幅 3.40%，成交量占比 4.83% → 实际权重占比 8.25%（**放大 1.71 倍**）

后果与经济含义相反：**振幅越大的 K 线（往往是消息面驱动的一次性长影线）越主导"筹码密集区"**，
而"筹码密集"本该描述价格在某一档位反复成交堆积。

正确做法是把成交量在覆盖区间内摊薄：`w / 覆盖箱数`，使 `sum(密度) == sum(成交量)`。
已实现于 `src/srlab/profile.py:volume_profile`，相对误差 4.5e-16，由 `test_volume_conservation` 守护。

### 2.2 多周期合并从未真正发生

`multi_timeframe_zones` 用 `(z.low, z.high)` 浮点元组做字典键来合并不同周期的区间：

```python
key = (z.low, z.high)
if key not in weighted: ...
```

实测 D1 + H1 + M15 共 9 个区间，`(low, high)` 键去重后仍是 9 个 —— **合并次数 = 0**。
所谓"大周期权重 2.0 / 3.0、小周期 0.5"的加权逻辑从未生效，`combined` 只是按
`strength × weight` 排序后的拼接。正确做法是按 **ATR 距离** 聚类合并。

### 2.3 历史胜率的统计口径无意义

`zone_history.analyze_zone_history` 逐根 K 线判断"是否触及"。实测单个区间在
8,403 根历史里录得 **1,430 次"触及"**（占全历史 17%），报出"守住率 91.8%"。

三个问题：

1. **重复计数**：价格在区间内盘整 20 天 = 20 次"触及事件"；
2. **不分方向**：从下方涨上来碰到"支撑"也算测试支撑，与支撑的定义相反；
3. **无冷却期**：连续触及无法合并成一次事件。

正确做法：事件去重（冷却 5 根）+ 接近方向校验（支撑要求上一根收盘在区上方）。
已实现于 `src/srlab/detectors.py:_touch_stats`，方向校验由 `test_label_direction` 守护。

### 2.4 阈值写死为百分比，跨标的完全不可比

守住判据写死 `min_close >= zone_lo * 0.98`（2%）。同一个 2% 阈值实测：

| 标的 | 现价 | ATR14 | ATR/价格 | 2% 相当于 |
|---|---|---|---|---|
| 000001 | 10.47 | 0.238 | 2.27% | 0.88 × ATR |
| 000002 | 2.99 | 0.124 | 4.16% | 0.48 × ATR |
| 000004 | 0.40 | 0.247 | 61.79% | **0.03 × ATR** |

在 000004 上 2% 不到 ATR 的 3%，一天必破，胜率虚低；在低波动股上接近 1 个 ATR，胜率虚高。
**所有阈值必须 ATR 化。**

### 2.5 区间宽度与波动率脱钩 —— 这是"高胜率"的真正来源

半高宽法给出的区间宽度实测为 **2.73 / 5.65 / 2.91 倍 ATR**（V1 全样本中位 2.21 ATR，
w120 配置 3.07 ATR）。一个 5.65 ATR 宽的"支撑区"跨越 13% 的价格空间，
价格几乎必然"触及"且必然"守住"。

这不是猜测，是可量化的：测试集上 V3 的区宽分层显示

| 区宽 | 守住率 | 触及后 10 日收益 |
|---|---|---|
| 0.34 ATR | 65.8% | **+1.05%** |
| 0.77 ATR | 70.7% | +0.78% |
| 1.20 ATR | 77.9% | **−0.19%** |

**区间越宽，"守住率"越高，而实际收益越低。** 于是"提高胜率"的最优策略就是把区间画得无限宽——
指标可以被算法白嫖。跨算法比较**必须固定区宽口径**（本方案统一用 ≤1.5 ATR）。

把 V1 也约束到 ≤1.5 ATR 后，13,361 个区间里只剩 **2,718 个**（20%），守住率从 87.4% 掉到 80.7%。

### 2.6 性能无法支撑回测

| 函数 | 单次耗时 |
|---|---|
| `find_dense_zones(window=40)` | 13.1 ms |
| `find_dense_zones(window=250)` | 61.8 ms |
| `analyze_zone_history` | **3.89 s** |

一次 200 标的 × 100 决策点的走查需要 **21.6 小时**。不向量化就没有回测，没有回测就没有优化。
向量化后（差分数组 + cumsum，`np.add.at`）全流程 200 标的 × 15,280 决策点 × 8 个检测器 = **630 秒**。

### 2.7 附带问题：数据适配器不认 A股 文件

`local_data_loader._parse_filename` 的正则是 `[A-Z0-9]+`，`000001_daily.parquet` 的 `daily` 是小写，
匹配失败；`list_instruments(A股目录)` 返回 **0 个品种**。且 A股 parquet 的 `time` 列单位是
**unix 秒 / 1000**（实测 000001 首根 = 670690 → 1991-04-03，与平安银行上市日一致），
不是秒；`tick_volume` 单位是**手**（100 股）。已在 `src/srlab/data.py` 适配。

---

## 3. 评测底座：让"可回测、可验证"有物理保障

优化的前提是有一把可信的尺子。这部分是整套方案里最不该省的。

### 3.1 结构性防泄漏

```python
ctx.high = sym.high[:t + 1]     # numpy 视图：检测器物理上拿不到 t 之后的数据
评测函数只读 sym.*[t + 1 : t + horizon]
```

唯一的难点是摆动枢轴：ZigZag 天生需要未来才能确认极值。解法是
**一次性算出全部枢轴并记录 `confirm_idx`（反向走出 k×ATR 的那一刻）**，
t 时刻只放行 `confirm_idx <= t` 的枢轴。这与"在 t 时刻实时计算"严格等价，
但把 O(n²) 降到 O(n)。

### 3.2 四个自动化测试（`python -m src.srlab.leakage`）

| 测试 | 验证什么 | 结果 |
|---|---|---|
| `test_pivot_causality` | 全序列枢轴 + confirm_idx 过滤 == 截断序列上重算 | PASS（12 个时点，1,060 个枢轴） |
| `test_no_future_dependence` | **把 t 之后的行情全部替换成随机垃圾，检测输出必须逐位不变** | PASS（8 时点 × 3 检测器） |
| `test_volume_conservation` | 成交量剖面体积守恒 | PASS（误差 4.5e-16） |
| `test_v1_equivalence` | V1 向量化复刻 == 原始 `find_dense_zones` | PASS（10 个时点） |
| `test_label_direction` | 从下方涨上来不算测试支撑 | PASS |

`test_no_future_dependence` 是最有价值的一个：**用户不需要读懂算法，只要这个测试通过，
就可以确信回测没有用未来数据**。

### 3.3 结果定义（这是最容易做错的地方）

**触及事件**：支撑要求 `close[t] > zone.high`（价格在区上方），此后首次 `low[j] <= zone.high`。
每个评测窗口只取第一次，天然无重复计数。

**结果判定**（先发生者胜出，从 j 起最多 10 根）：
- 突破：`close[m] < zone.low − 0.5 × ATR_t`
- 守住：`high[m] >= zone.high + 1.0 × ATR_t`
- 都没发生 → 未决（不进胜率分母，单独统计占比）

`ATR_t` 是**决策时刻**的 ATR，阈值本身不含未来信息。

**进场基准必须用 `close[j]`，不能用区间边沿。**
因为触及的定义就是 `low[j] <= zone.high`，以 `zone.high` 为进场价时 MAE 天然有
`>= (zone.high − low[j])` 的正下限，而 MFE 没有对称下限 —— 冒烟测试中全体检测器
`edge_atr` 一律为负，就是这个构造性偏差造成的。`close[j]` 在 A股 T+1 下真正可成交，且多空对称。

### 3.4 对照组：没有对照的胜率毫无意义

在均值回复的市场里，**任何**位于现价下方 2~3 ATR 的价格带都会有较高的"触及后反弹"比例。

| 对照组 | 构造 | 检验什么 |
|---|---|---|
| `placebo_shift_vN` | 把 **该算法自己**的每个位随机平移 0.8~2.0 ATR（方向随机），宽度、数量、距离量级全保留 | 算法是否真的知道位在**哪个具体价格** |
| `placebo_fixed` | 距现价固定 ±1.5 / 2.5 / 4.0 ATR | "只知道距离"能达到什么水平 |

平移幅度取 0.8~2.0 ATR 是有讲究的：太小（<0.5 ATR）对照组会顺带命中真实位，提升度被人为压低；
太大（>3 ATR）距离分布被破坏，比较不再同口径。

**每个候选算法都配自己的平移对照**，不能拿 A 的对照去比 B（这是我第一版报告里的方法学错误）。

### 3.5 必须控制距离

测试集实测：阻力位守住率随距现价距离从 **68.4%（0.84 ATR）升到 84.9%（4.20 ATR），+16.5pp**，
是所有因子里最强的主效应。因此"某算法守住率更高"完全可能只是因为它偏爱把位放得更远。

结论层面一律使用 **Cochran–Mantel–Haenszel 分层检验**：按距离分 6 档，每档构造
2×2 表（算法 × 守住/突破），合并各档证据。

---

## 4. 实测结果

> **本节数字为复权修复后重跑的结果**（`out/final/report.txt`，2026-08-15）。
> 标的池 = 有权威复权因子的股票，train 130 / test 130（按代码 md5 分桶互斥），
> 后复权（严格因果），19,867 个决策点，测试集 83,661 条关键位观测、44,618 次已判定触及。
> 修复前（200 只未复权）与修复后（260 只已复权）的方向性结论完全一致。

### 4.1 各算法总览（测试集，区宽 ≤ 1.5 ATR 同口径）

| 算法 | 关键位数 | 触及率 | 守住率 [95%下界] | 触及后10日收益 |
|---|---|---|---|---|
| V1_density（现网） | 3,486 | 0.608 | 0.801 [0.780] | +0.03% |
| V1_density_w120 | 1,078 | 0.494 | 0.775 [0.730] | +1.39% |
| V2_fusion | 70,616 | 0.602 | 0.762 [0.757] | −0.10% |
| **V3_fusion** | 83,661 | 0.601 | 0.758 [0.754] | −0.11% |
| placebo_shift_v3 | 79,821 | 0.615 | 0.759 [0.755] | −0.11% |
| placebo_shift_v1 | 4,182 | 0.654 | 0.778 [0.760] | −0.21% |
| placebo_fixed | 119,202 | 0.563 | 0.749 [0.745] | −0.12% |

关键观察：把 V1 约束到 ≤1.5 ATR 后，它只剩 **3,486** 条关键位，而 V3 有 83,661 条 ——
V1 绝大多数输出的区间宽度超过 1.5 ATR，根本进不了这个同口径比较。
V1 在不加宽度约束时守住率仍是 87% 量级（中位区宽 2.2 ATR），这就是现网界面上那个好看的数字的来源。

### 4.2 核心负面结论：定位能力无法通过结果检验

控制距离后（CMH 分层，测试集）：

| 对比 | support 守住率提升 | p | resistance 守住率提升 | p |
|---|---|---|---|---|
| V3 vs 自身平移对照 | −0.39pp | 0.38 | −0.31pp | 0.44 |
| V2 vs 自身平移对照 | +0.41pp | 0.40 | −0.26pp | 0.58 |
| **V1 vs 自身平移对照** | **+5.33pp** | **0.0053** | −1.61pp | 0.53 |
| V3 vs 固定距离对照 | **+3.02pp** | **<0.0001** | −0.08pp | 0.37 |

**读法**：把 V3/V2 的"支撑位"随机平移 1~2 个 ATR，它触及后守住/突破的概率几乎不变。
说明这两个算法给出的关键位，作为"精确价格"的信息量在噪声水平以下；
它们的真实分辨率就是 1~2 个 ATR。

**一个在大样本下才显形的例外（新增）**：V1 那些**罕见的窄区间**是真有定位信息的 ——
support 侧比自己的平移对照高 **+5.33pp（p=0.0053）**，守住率 81.1%。
但代价是它只在约 4% 的决策点上产出这种窄区间（19,867 个决策点只有 3,486 条 ≤1.5 ATR 的位），
其余全是 2~5 ATR 的宽区间。
换句话说：**V1 的密度峰值在偶尔收敛成一个窄峰时确实抓到了真东西，但绝大多数时候没有。**
这条线索值得做成 P3 实验（见 §5 P3 表），但不能作为现网默认 ——
它的召回率太低，且 V1 vs V3 的直接对比里 V1 只有 3,486 个样本、V3 有 83,661 个，
V1 的高守住率里包含很强的选择效应（只有形状最干净的位才会进入这个子集）。

V3 显著优于固定距离对照（support +3.00pp, p<1e-4）说明**在同一距离档内**，
算法选的位置确实比机械网格好；但**无条件地**看，固定网格把位放在
1.5~4 ATR 这个未来摆动极值的众数区间、且每个决策点铺满 6 个位，几何召回率反而最高。
**距离的选择比价格的选择重要得多**，这一点必须诚实地告诉用户。

### 4.3 概率校准：现网的"守住率"必须下线

测试集（训练集拟合保序回归 → 测试集验证）：

```
Brier = 0.1828    常数预测 Brier = 0.1834    ECE = 0.0029
```

**与常数预测持平（改善 0.0006）= 零区分度。** 校准表把 44,618 个样本中的 44,616 个
压进 [0.6,0.8) 和 [0.8,0.9) 两个箱。任何形如"历史守住率 91.8%"的展示都是误导，因为：

1. 它的分母是重复计数的伪事件（§2.3）；
2. 它的高低主要由区宽决定（§2.5）；
3. 模型对单个位的守住概率没有区分能力。

### 4.4 单因子分析：哪些证据真的有信息

测试集 support（已触及，按五分位分层，Q5 − Q1）：

| 证据 | 守住率差 | 收益率差 | p | 判定 |
|---|---|---|---|---|
| **历史触及事件数** | **+7.11pp** | **+0.10pp** | <1e-7 | ✅ 唯一 hold 与收益**同向**的证据，且在**每个距离档内**都单调 |
| 成交量剖面强度 | +6.77pp | **−0.91pp** | <1e-7 | ⚠️ 预测"停滞"，反向预测"收益" |
| 收盘价堆积强度 | +6.58pp | **−0.84pp** | <1e-7 | ⚠️ 同上 |
| 枢轴聚集强度 | +2.27pp | −0.15pp | 0.069 | ❌ 不显著 |
| 整数关口贴近度 | −0.48pp | +0.51pp | 0.70 | ❌ 无信息（阻力侧显著为**负**，p=2e-4） |
| 近期触及新鲜度 | −1.62pp | −1.02pp | 0.22 | ⚠️ **阻力侧强烈反向**：越新鲜越弱（84.2% → 70.5%，p<1e-34） |

三个关键发现：

**(1) "测试次数越多的位越强"是真的。** 这是技术分析里少见的能通过分层检验的说法：
守住率 69.6% → 76.8%，且在 (2.87, 5.0] ATR 距离档内收益率从 +0.67% 升到 **+1.46%**。

**(2) 成交量密集区预测"价格会停"，不预测"进场能赚"。**
这是本次分析最有价值的发现，也是现有产品把两件事彻底混为一谈的地方。
经济解释：重仓套牢盘确实能止住下跌（守住率高），但同样封死了反弹空间（收益率低）。
`s3_vp_only` 评分守住率差 +8.98pp（最高），收益率差 **−0.88pp，单调性 −0.80** —— 干净的反向。

**(3) 现有直觉里"刚被测试过的位更有效"是错的，方向正好相反。**
阻力位越新鲜越弱（p<1e-34）。物理意义合理：价格反复摩擦阻力位往往是即将突破的前兆。
我的 V2 里 `w_recency = +0.35`，符号完全用反了。

### 4.5 评分搜索：两个因子就够了

在 train / test 两个互斥股票池上各约 38,000 个已触及关键位：

| 候选评分 | support 守住率差(test) | support 收益差 | resistance 守住率差 | resistance 收益差 |
|---|---|---|---|---|
| s0 只用事件数 | +6.69pp (mono 1.00) | +0.24pp | +5.56pp | +0.38pp |
| **s1 事件数 + 陈旧度** | **+6.11pp (mono 1.00)** | **+0.41pp** | **+7.36pp (mono 1.00)** | **+0.81pp** |
| s2 s1 − 成交量剖面 | +4.62pp | +0.67pp | +6.45pp | +0.57pp |
| s3 只用成交量剖面 | +8.98pp | −0.88pp (mono −0.80) | +4.18pp | +0.20pp |
| s4 只用枢轴聚集 | −1.10pp (mono −0.10) | −0.27pp | +3.21pp | −0.18pp |
| s5 只用 Wilson 守住率 | +6.97pp | train/test **符号翻转** | +3.19pp | 不稳定 |
| s6 完整 5 因子（旧 V3） | +5.67pp | +0.58pp | +5.96pp | +0.56pp |

**s1 用 2 个因子达到甚至超过 5 因子版本**，且训练集（+6.42 / +7.15pp）与测试集
（+6.11 / +7.36pp）高度一致 —— 无过拟合迹象。

```python
score = log1p(历史触及事件数) + 0.8 × (1 − 近期新鲜度)
```

按此配置重跑全流程（复权后），测试集五分位表：

| 分位 | n | 守住率 | 触及后 10 日收益 |
|---|---|---|---|
| Q1 | 9,231 | 0.7171 | **−0.363%** |
| Q2 | 8,630 | 0.7483 | +0.014% |
| Q3 | 8,930 | 0.7695 | +0.180% |
| Q4 | 8,905 | 0.7720 | +0.111% |
| Q5 | 8,922 | **0.7844** | **+0.117%** |

守住率单调 +6.7pp，收益率 Q1→Q5 由负转正。**这是整套方案里唯一可以直接变现的能力。**

分方向的标定表（已落盘 `data/score_calibration.json`，界面直接引用）：

| 方向 | 分位 | n | 守住率 [95%下界] | 触及后10日收益 |
|---|---|---|---|---|
| support | Q1 | 3,856 | 0.6979 [0.6832] | +0.415% |
| support | Q3 | 4,219 | 0.7409 [0.7275] | +0.486% |
| support | **Q5** | 4,000 | **0.7645 [0.7511]** | **+0.779%** |
| resistance | Q1 | 4,879 | 0.7311 [0.7185] | −1.208% |
| resistance | Q3 | 4,927 | 0.7938 [0.7823] | −0.424% |
| resistance | **Q5** | 4,709 | **0.7961 [0.7844]** | −0.684% |

support 侧守住率（69.8%→76.5%）与收益率（+0.42%→+0.78%）**同向单调**，
这正是两因子排序分被选中的理由。界面上显示的"该档实测守住率"就是这张表的值。

### 4.6 几何精度：定位能力的唯一正面证据

"未来 40 根内真实发生的摆动极值，离预测关键位有多远"（每侧取分数最高的 3 个，同口径）：

| 算法 | 中位误差 (ATR) | 召回@0.5ATR | 召回@1.0ATR | 平均输出位数 |
|---|---|---|---|---|
| V1_density | 1.765 | 0.184 | 0.339 | 0.87 |
| V1_density_w120 | 2.020 | 0.152 | 0.287 | 0.61 |
| V2_fusion | 1.210 | 0.258 | 0.453 | 3.55 |
| **V3_fusion** | **1.093** | **0.286** | **0.479** | 4.21 |
| placebo_shift_v3 | 1.373 | 0.250 | 0.419 | 4.02 |
| placebo_shift_v1 | 1.921 | 0.166 | 0.311 | 1.21 |
| placebo_fixed | 0.718 | 0.402 | 0.567 | 6.00 |

V3 比 V1 改善 **38%**，比自己的平移对照改善 **20%** —— 这是唯一一处定位能力确实优于对照组的地方。
但 `placebo_fixed` 仍然最好，原因已在 §4.2 说明（6 个位铺满摆动众数区间）。
**诚实的表述是：本算法能把关键位定位到约 1.1 个 ATR 的精度，比原算法好 39%，但不比"按 ATR 铺网格"更好。**

---

## 5. 优化方案（按投入产出排序）

### P0 —— 必做，纠正错误与误导（1~2 天）

| # | 改动 | 依据 | 位置 | 状态 |
|---|---|---|---|---|
| P0-1 | 成交量按覆盖箱数摊薄，保证体积守恒 | §2.1，放大 15.8 倍 | `profile.volume_profile` | ✅ |
| P0-2 | 分箱宽度改为 `max(tick, 0.25×ATR)` | §2.4，跨标的可比 | `profile.make_bins` | ✅ |
| P0-3 | 所有阈值 ATR 化，删除写死的 2% | §2.4，0.03~0.88 ATR | `detectors._touch_stats` | ✅ |
| P0-4 | 区宽夹逼到 `[0.3, 1.2] × ATR` | §2.5，原为 2.2~5.7 ATR | `detectors._zone_bounds` | ✅ |
| P0-5 | 触及事件去重（冷却 5 根）+ 接近方向校验 | §2.3，1,430 → 个位数 | `detectors._touch_stats` | ✅ |
| P0-6 | **界面下线"历史守住率 XX%"** | §4.3，Brier 与常数预测持平 | `templates/index.html`、`app.py` | ✅ |
| P0-7 | 多周期按 ATR 距离聚类合并，替换浮点元组键 | §2.2，合并次数 = 0 | `density_analyzer.multi_timeframe_zones` | ✅ |
| P0-8 | 向量化（差分数组 + `np.add.at`） | §2.6，21.6h → 468s | `profile`、`detectors` | ✅ |
| P0-9 | A股 数据适配（文件名、time×1000、手→股） | §2.7，识别 0 个品种 | `srlab/data.py`、`local_data_loader` | ✅ |
| P0-10 | **复权**（权威因子缓存 + 缺失时报错/告警） | §7.5，15.2% 决策点被污染 | `srlab/adjust.py` | ✅ |

P0-6 是唯一一条产品层面的改动，也是最重要的一条：**继续显示一个零区分度的胜率，
比不显示任何数字更糟糕**，因为用户会照着它下单。

实施后的界面契约（已验证 `success_rate` / `overall_score` / `level_label` / `risk_score` /
`alerts` / `held_count` / `strength` / `volume_pct` / `touch_count` 全部下线）：

```
每个关键位输出：
  中心价 / 价格带 / 带宽(ATR) / 距现价(% 与 ATR 双单位)
  历史被测试次数            <- 唯一实测既能预测守住率又能预测收益的证据
  排序档 Q1~Q5              <- 该位落在历史哪一档
  该档样本外实测守住率        <- 来自 data/score_calibration.json，可追溯到回测
  停滞倾向 Q1~Q5            <- 成交量剖面驱动，只用于放止损/目标，不参与排序
  多周期共振                 <- ATR 距离聚类的结果
  复权状态 / 未复权告警       <- 逐行暴露，批量扫描表格也标注
```

`data/score_calibration.json` 的分位边界在 **train** 股票池上确定，
每档的守住率与收益在**互斥的 test** 股票池上统计 ——
所以界面上显示的"Q5 档实测守住率 77.0%"是样本外真实统计，不是模型预测的概率。

### P1 —— 核心能力，把"排序"和"停滞"分开（3~5 天）

| # | 改动 | 实测收益 | 状态 |
|---|---|---|---|
| P1-1 | 排序分改为 `log1p(触及事件数) + 0.8×陈旧度`，删除枢轴/整数关口/Wilson 权重 | 守住率 Q1→Q5 +6.9pp，收益 −0.26%→+0.23%（§4.5） | ✅ |
| P1-2 | 新增独立的 `p_stall`（成交量剖面 + 收盘堆积 + 区宽），**只用于放止损/目标位，不参与排序** | 解决"分数越高守住率越高但收益越低"的自相矛盾（§4.4-2） | ✅ |
| P1-3 | 修正 `recency` 符号：陈旧 = 强，新鲜 = 弱 | 阻力侧 84.2% vs 70.5%，p<1e-34（§4.4-3） | ✅ |
| P1-4 | 只输出 `[0.5, 5.0] × ATR` 距离带内的位，每侧最多 3 个 | 距离带外的位交易上无意义；且距离是最强主效应（§4.2） | ✅ |
| P1-5 | 界面把 `n_events`（测试次数）作为主排序列展示 | 唯一通过分层检验的证据（§4.4-1） | ✅ |
| P1-6 | 线上引擎直接复用回测验证过的 `V3Fusion`，不复制算法 | 杜绝"回测一套、线上另一套" | ✅ `src/sr_engine.py` |

关键设计决策：**把一个分数拆成两个。**

```
排序分 edge     = log1p(触及事件数) + 0.8 × 陈旧度        → 决定看哪几个位
停滞分 p_stall  = 成交量剖面 + 0.8×收盘堆积 + 0.3×区宽    → 决定止损/目标放哪
```

V2 把两者混成一个 `score`，导致"分数越高守住率越高但收益越低"，
校准 Brier 与常数预测完全相同 —— 这是 §4.3 零区分度的直接原因。

### P2 —— 表达方式，从"线"改成"带"（2~3 天）

实测分辨率是 1~2 ATR（§4.2），产品必须如实表达：

- 关键位一律画成**带**，宽度按 ATR 标注（"支撑带 10.12–10.38，宽 1.1 ATR"）
- 不显示概率，显示**分位标签**（"排序 Q5 / 共 5 档"）+ **触及次数**（"历史被测试 12 次"）
- 距离一律双单位（"距现价 −2.3%（−1.8 ATR）"）
- 图上叠加 `placebo_fixed` 的 ±1.5/2.5/4.0 ATR 参考线，让用户自己看出
  "算法的位是否比机械网格更值得信"

### P3 —— 值得试，但先做实验（各 2~3 天，做完看数据再决定是否上线）

| 假设 | 实验设计 | 为什么值得试 |
|---|---|---|
| 用 15min/60min 数据构造**真正的**成交量-价格剖面 | 数据集有 15min/60min，但只覆盖 2024-06 起约 2 年 | 日线的 [low,high] 摊薄只是近似；真实 VAP 可能提升定位精度 |
| 距离带内的**条件收益模型**（距离 × 趋势 × 波动率） | 用 `dist_atr`、EMA 斜率、ATR/价格 做分层 | §4.2 显示距离是最强主效应，把它建模比继续抠位置更有前途 |
| 突破方向预测（而非守住） | 把标签换成"突破后延续 vs 假突破" | 守住率已饱和在 76%，突破侧可能有未开发的区分度 |
| 多周期共振 | P0-7 已修好（实测 D1+H1+M15 在 000001 上产生 2 次真实合并），检验 `tf_count>1` 是否带来增量守住率 | 现有实现从未生效，等于从未验证过 |
| **V1 窄峰作为过滤器** | 把 V1 的"密度峰收敛成 ≤1.5 ATR 窄区间"当作 V3 的额外过滤条件，看能否在保留 V3 召回的同时拿到 V1 的 +5.33pp | §4.2 新发现：V1 的罕见窄区间是**唯一**在守住率上显著优于自身平移对照的口径（p=0.0053）；但它只覆盖约 4% 的决策点，需要验证这是真信号还是选择效应 |

### 明确不建议做的事

- ❌ **继续调密度峰值检测的参数**（prominence / width / distance / bins）。
  §4.2 已证明整个"精确定位"方向的信息量在噪声水平以下，调参不会改变结论。
- ❌ **加入整数关口权重**。support p=0.70，resistance 显著为负（p=2e-4）。
- ❌ **加入枢轴聚集权重**。support 单调性 −0.10，消融显示去掉它反而更好。
- ❌ **做空阻力位**。所有算法在 resistance 侧逐笔期望值均为负（V3 −0.845%，t=−9.87），
  这是 A股 长期上行漂移，不是算法问题。阻力位只应用于**止盈**。

---

## 6. 代码结构

```
src/srlab/                      新增的实验室（与现网 src/*.py 解耦，可独立演进）
├── data.py          A股/MT5 parquet 适配、因果 ATR、标的池 md5 分桶切分
├── base.py          Level / Ctx（因果快照）/ Detector 接口 / NMS
├── pivots.py        因果 ZigZag（confirm_idx 机制）
├── profile.py       体积守恒剖面、ATR 分箱、时间衰减、高斯核、峰值检测
├── detectors.py     V1Density(基线复刻) / V2Fusion / V3Fusion / 两种 Placebo / 消融
├── labeling.py      触及事件、前瞻结果标注、未来枢轴真值
├── metrics.py       Wilson、两比例 z、CMH 分层检验、保序校准、机械回测
├── walkforward.py   无泄漏走查运行器 + 统一后处理
└── leakage.py       5 个防泄漏/正确性测试

scripts/
├── run_eval.py       走查回测主入口（10 个报告小节）
├── factor_analysis.py 单因子分层分析（不需重跑回测）
└── score_search.py    候选评分搜索（train 选参 / test 汇报）
```

现网 `src/density_analyzer.py`、`src/zone_history.py` **未改动**，
`V1Density` 是它的忠实向量化复刻（`test_v1_equivalence` 逐点对齐验证），
因此 P0/P1 可以在 `srlab` 里验证通过后再切换 `app.py`，风险可控。

---

## 7. 用户如何自己验证

```bash
cd D:\cl\Detect_support_and_resistance_levels

# 0) 复权：确认本地数据口径 + 构建权威因子缓存（首次必做，联网）
python scripts/check_adjustment.py            # 判定不复权/前复权/后复权
python scripts/build_factors.py --n 400       # 建缓存；并行见下，勿超 3 路
#    并行加速（3 路，各约 12 分钟）：
#    python scripts/build_factors.py --n 400 --slice 0/3
#    python scripts/build_factors.py --n 400 --slice 1/3
#    python scripts/build_factors.py --n 400 --slice 2/3
#    python scripts/build_factors.py --merge  # 合并分片
python scripts/validate_adjustment.py         # 污染度 + 尺度不变性
python scripts/eval_heuristic.py              # 离线启发式的精度（为何不能用）

# 1) 验证"没有用未来数据" —— 7 个测试，约 60 秒
python -m src.srlab.leakage

# 2) 冒烟测试：20 只股票，约 35 秒
python scripts/run_eval.py --quick --out out/smoke

# 3) 完整评测 + 导出界面标定表，约 8 分钟
python scripts/run_eval.py --n 150 --out out/final

# 4) 单因子分析：哪些证据真的有信息（读第 3 步的落盘结果，秒级）
python scripts/factor_analysis.py out/final

# 5) 评分搜索：确认 2 因子 vs 5 因子（秒级）
python scripts/score_search.py out/final

# 6) 起服务看界面
python app.py     # http://127.0.0.1:5000
```

产出：

- `out/final/report.txt` —— 10 节汇总报告（人可读）
- `out/final/levels_{train,test}.parquet` —— 逐关键位明细，每行是一个关键位在一个决策点的
  全部证据分量 + 前瞻结果。**可以自己拿去做任何交叉分析，不需要重跑回测。**
- `out/final/geo_{train,test}.parquet` —— 逐决策点几何精度

自查清单：

1. `leakage.py` **7/7** 通过 → 无未来信息、复权尺度不变、V1 复刻等价
2. 报告第 2.4 节所有算法的 `med_width_atr` 是否一致 → 区宽同口径
3. 报告第 4b 节 `cmh_p` → 控制距离后是否还有提升
4. 报告第 6 节 `Brier` vs `基准(常数预测) Brier` → 概率有没有区分度
5. 报告第 7 节 Q1→Q5 的 `hold_rate` 与 `fwd_ret_pct` 是否**同向**单调 → 排序分是否可用
6. 报告第 11 节的分档表是否与界面显示的"该档实测守住率"一致 → 界面数字可追溯
7. 界面汇总表头部是否出现"⚠ N 只未复权" → 复权状态没有被静默隐藏

---

## 7.5 复权问题（已查清并处理）

上一版把"未确认复权口径"列为最大未知项。现已用外部数据源查清。

### 结论：本地数据是**不复权**的

三条独立证据（`python scripts/check_adjustment.py`）：

| 判据 | 做法 | 结果 |
|---|---|---|
| 与 baostock 三种口径逐日比对 | 比 `local/ref` 的**变异系数**（前复权基准日不同会整体等比缩放，不能直接比数值） | 与 **不复权** 10/10 只吻合（4 只数值完全相同，6 只 cv 3e-5~9e-4，来自 parquet 的 float32 存储）；与前/后复权 cv 0.61~2.02 且除权日有 ~50% 阶跃 |
| 本地跳空检测（**不依赖任何外部源**） | A股 涨跌幅上限 ±10%（创业板/科创板 2020-08 后 ±20%），单日 >±21% 只能是除权 | 每只股票 1~18 次，最差 **−56.7%**（600519 于 2006-05-25，茅台 10转10 除权日） |
| 因子结构 | 后复权/不复权 相除应为分段常数 | 是，000001 在 5343 根上仅 19 个阶跃 |

### 污染程度：15.2% 的决策点

`python scripts/validate_adjustment.py`，300 只样本、回看 750 根、区间 ≥2012：

- **152,983 / 1,007,175 = 15.2%** 的决策点，回看窗口内含除权跳空
- 分布：中位 17.6%，P90 38.2%，最重个股 **69.4%**（000639）
- 识别到的除权次数：中位 3 次/只，293/300 只至少有 1 次

所以这不是可以忽略的边角问题，上一版回测的 15% 决策点确实被污染了。

### 处理方式：权威因子缓存（不是启发式）

先实现了纯离线的**开盘缺口启发式**：除权参考价按 `open/prev_close` 计算，缺口发生在开盘，
而涨跌停最多 ±limit，所以超过 limit 的开盘跌幅只能是除权。
阈值从固定 22% 改成**按板块与日期取当日涨跌幅上限 + 0.5%**
（主板 10.5%、创业板 2020-08-24 后 20.5%、科创板 20.5%、北交所 30.5%），
并跳过距上一根 >10 自然日的 bar（停牌复牌不是除权）。

但实测精度不够（`python scripts/eval_heuristic.py`，以 50 只已有权威因子的标的为真值）：

| 口径 | 残余最大断层 中位 / P90 | 比值 cv 中位 | 漏检率 |
|---|---|---|---|
| 不复权 | 0.341 / 0.509 | 0.371 | — |
| 启发式 阈值 0.22 | 0.093 / 0.282 | 0.097 | 74.9% |
| 启发式 按板块限幅 | **0.047 / 0.212** | 0.067 | 50.3% |

即便改进后，仍有 **64%（32/50）的股票残余 >3% 的价格断层**（对典型个股 ≈3~4 个 ATR，
足以毁掉一个支撑位的判定），且会**误判**：000004 真实除权 0 次，启发式识别出 10 次，
反而制造了 12% 的人为断层。

因此最终方案是**权威因子缓存**：

```
factor_hfq(t) = 后复权收盘(t) / 不复权收盘(t)     # 同一数据源两次查询相除
```
落盘到 `data/adj_factors.parquet`，并用 `_piecewise_constant()` 去掉两位小数带来的
~0.1% 量化噪声（真实因子是分段常数）。

`ashare_load(adjust='factor')` 在**因子缺失时直接抛 `MissingFactorError`**，
不静默回退启发式 —— 静默地用错误的复权数据回测，比不复权更危险。
`build_universe` 也只保留有因子的标的，这是评测标的池的显式定义。
界面路径（`load_kline`）则降级为不复权但**必须显示告警**，用户能看见"此标的未复权"。

数据源踩坑：akshare/东方财富在连续约 800 次请求后按 IP 封禁
（所有 CDN 节点、curl_cffi 全部浏览器指纹都被拒），只能用 baostock（~11s/只）。
`scripts/build_factors.py --slice i/n` 支持多进程分片，6 进程并行把 400 只从 4 小时压到约 12 分钟。

### 回测用后复权，界面用前复权

前复权锚定"今天"，因子依赖 t 之后的除权，**不是因果的**；
后复权锚定序列起点，`factor[t]` 只由 ≤t 的事件决定，**严格因果**。

两者只差一个全局常数，而本算法的分箱/阈值/区宽全部以 ATR 为单位，是尺度不变的。
这一点不是靠论证，而是由 `leakage.test_adjust_scale_invariance` 逐点验证：
6 个时点上 V3 输出的关键位中心严格满足 `center_qfq == center_hfq × k`（k=0.238034），
且 `dist_atr` / `width_atr` 完全一致。

> 注：V2 **故意**不满足尺度不变性，因为它的整数关口项依赖绝对价位 ——
> 这也是 V3 删掉该项的原因之一（实测 support p=0.70 无信息，resistance 显著为负）。

两个被测试抓到的真实 bug：
1. `apply_factor` 对早于因子起点的 bar 原本填 1.0，会在缓存起点制造巨大断层 → 改为首值回填（bfill）。
2. 最初断言"后复权因子单调不减"，实测 2/50 只存在真实下降（000001 单次 −16.8%，
   对应深发展/平安银行换股）。**因果性不等于单调性**，断言改为检查首值归一与分段常数性。

---

## 8. 已知局限

1. **只在 A股 日线上验证。** 期货、外汇、分钟级可能不同，尤其是缺乏涨跌停和 T+1 约束的品种。
2. **回测区间 2012–2026 包含 2015 和 2024 两轮极端行情**，未做分市场状态的稳健性检验。
3. **复权因子覆盖范围受限。** 因子缓存需要联网构建（baostock ~11s/只），
   未覆盖的标的在回测里会被直接排除、在界面上会显示未复权告警。
   全市场 4272 只全量构建约需 2 小时（6 进程并行）。
4. **成交量未随复权调整。** 送转会改变股数，现金分红不会，仅凭价格因子无法区分两者。
   成交量在本算法里只作为 250 根窗口内的相对权重，且有 60 根半衰期衰减压制旧数据，
   因此判断影响可接受 —— 但这是一个未量化的近似。
5. **15min/60min 数据仅覆盖 2024-06 起约 2 年**，且时间戳有 ±1000 秒量化误差
   （实测 15min 文件出现重复时间戳），只能保证顺序正确，不适合精确对齐。
   分钟级数据目前**不做复权**（因子按日频给出），跨除权日的分钟级分析不可靠。
6. **交易回测是逐笔独立的**，没有资金曲线、仓位管理和容量约束，
   目的是隔离信号质量，不能直接当策略收益看。
7. **`edge_atr` 在多数配置下为负**，说明"在支撑触及处做多、10 根后平仓"这个机械规则本身
   不是一个好策略；本方案的结论限于"关键位识别质量"，不构成策略建议。
8. **标的池规模由因子覆盖决定。** §4 用的因子缓存有 386 只，过滤后 train 130 / test 130。
   全市场 4272 只需继续跑 `build_factors.py`。已验证三个规模
   （复权前 100/100、复权后 75/75、复权后 130/130）方向性结论完全一致；
   但 §4.2 里 V1 窄区间的 +5.33pp 只在最大样本下才显著，说明部分结论对样本量敏感。
9. **`--slice` 并行度不宜超过 3。** 实测 6 个 baostock 会话并发时，
   3 个正常完成、1 个静默挂起、2 个登录失败（错误码 10002007）。
10. **界面批量扫描会包含未复权标的。** 4272 只中当前只有 386 只有因子，
    其余降级为不复权并逐行标注"未复权"。这些行的关键位判定不可靠，
    汇总表头部会显示"⚠ N 只未复权"。
